= bootstrap_form_for @article, :html => { :class => 'form-horizontal' } do |f|
  = f.text_field :title
  = f.text_area :description
  = f.text_area :content
  
  .form-actions
    = f.submit t('.submit'), :class => 'btn btn-primary'
    = link_to t('.cancel'), articles_path, :class => 'btn'
